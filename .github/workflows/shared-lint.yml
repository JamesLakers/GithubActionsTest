name: Shared-Lint
on: 
  workflow_call:
jobs:
    lint:
      runs-on: ubuntu-latest
      steps:
        - name: Get code
          uses: actions/checkout@v3
        - name: Install dependencies
          uses: ./.github/actions/cache
        - name: Lint code
          run: npm run lint
